

public class GraphColouringConstraintStore {
	
	private Graph graph;
	
	public GraphColouringConstraintStore(Graph graphIn) {
		graph = graphIn;
	}

	
	//loop through the constraints
		//get each constraint to prove that it's still feasible
		//if all constraints are feasible then get them to see if they can prune the search space
		//(i.e. remove any values from the domain of any of the decision variables)
	
	
}
